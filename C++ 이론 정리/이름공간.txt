이름 공간이란 프로젝트시 같은 함수이름으로 인한 오버로딩을 방지하기 위해 c++에서 제공하는 것이다.

namespcae A
{
	void B()
	{
		...
	}
}


이렇게 선언이 가능하고 이름 공간 A에 넣어둔 B를 호출할려면 

A::B(); 로 호출하면 된다. 

혹은 위에서 
using std::cin;
using std::cout;
using A::B;

이런식으로 선언해두면 차후에 B();만 써도 자동으로 이름 공간 A의 B를 호출하게 된다.