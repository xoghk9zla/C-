다형성구현.cpp에서
 - 메뉴에서 특정 클래스 지우기를 선택한다.
 - 어떤 도형을 지울것인가 선택한다. 
 - 예를 들어 원을 지운다고 선택하였다면
 
ShapeManager 클래스에
 - deleteClass( int id ) 라는 이름의 멤버함수를 만든다.
    이때 Shape 클래스의 설계에 따라 각 도형은 자신이 어떤 종류인가를 표시하는 값을 갖도록 할 수 있다.
    예를 들어 int id; 라는 값을 갖도록 했다면
    그리고 원 = 0, 사각형 = 1 과 같이 값을 주었다면
     
다형성구현.cpp에서
- sm.deleteClass( 0 )을 호출한다.
 
ShapeManager 클래스의
- deleteClass( int id ) 멤버함수에서
   for ( 매니저내의 모든 도형 )
        if ( 현재도형.id == 0 )
            delete( index );      // 인덱스 지우기를 이용하여 현재 도형을 지운다.
 
이 정도 하면 되겠습니다.
 
또는 dynamic_casting을 이용하여 원하는 클래스의 도형을 삭제할 수도 있겠습니다